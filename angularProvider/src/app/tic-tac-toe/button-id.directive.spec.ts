import { ButtonIdDirective } from './button-id.directive';

describe('ButtonIdDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonIdDirective();
    expect(directive).toBeTruthy();
  });
});
