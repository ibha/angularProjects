import { ApplyCssDirective } from './apply-css.directive';

describe('ApplyCssDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplyCssDirective();
    expect(directive).toBeTruthy();
  });
});
