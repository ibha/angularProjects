import { AddNewElementDirective } from './add-new-element.directive';

describe('AddNewElementDirective', () => {
  it('should create an instance', () => {
    const directive = new AddNewElementDirective();
    expect(directive).toBeTruthy();
  });
});
